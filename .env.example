PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Opcional, solo si quieres generar /galeria/[slug] en local (Project Settings → API → service_role key).
# Nunca la subas al repo ni la pegues en un chat — bypassa toda RLS.
# SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key
